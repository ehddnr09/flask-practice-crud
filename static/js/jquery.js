/* $(".list-group-item-action").click(function () {
  let product_title = $(this).attr("id");
  $.get("/detail?title=" + product_title).then(function (data) {
    $("#detailModalLabel").text(data.title);
    $("#detailModalContent").html(data.content);
    $("#detailModal").modal("show");
  });
});*/
